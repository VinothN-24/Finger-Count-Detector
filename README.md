# Finger-Count-Detector
Finger Count Detection Dashboard — A real-time web dashboard that uses AI hand-tracking (MediaPipe) via webcam to detect hands and count extended fingers live. It tracks 21 hand landmarks per hand, showing total count, left/right split, per-finger status, FPS, and reading history — all processed in-browser, no data sent externally.
